export const useSyncTransactions = () => {
  /**
   * @todo
   * Cuando la autenticacion sea valida
   * se verifica si hay transacciones pendientes en localStorage
   * si hay se solicita confirmacion para sincronizarlas
   */
};
